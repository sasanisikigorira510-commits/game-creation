# ストーリー / チュートリアル設計 2026-06-18

## 目的

現在の `WitchTowerGame` は、モンスター召喚、5体編成、ダンジョン攻略、仲間化、装備、配合、図鑑、ショップまで機能が増えている。

そのため、ストーリーは長い会話劇ではなく、プレイヤーが最初に触る操作そのものを「契約炉を拠点に、各地のダンジョンへ探索隊を送り出す儀式」として見せる方針にする。

目標は次の3つ。

- 初回3〜5分で、召喚、編成、バトル、報酬、強化の意味が分かる
- 既存の `HomeScene -> GachaScene -> FormationScene -> BattleScene` 導線に自然に乗せる
- 周回、敗北、仲間化、配合、装備品質、装備強化が世界観上も納得できる

## 現状前提

確認した主な実装前提。

- 起動後は `BootScene -> HomeScene`
- ホームから `BattleScene`、`FormationScene`、`EquipmentScene`、`FusionScene`、`GachaScene` へ遷移する
- 新規セーブは `FreeGachaStones = 3000`
- 召喚は `契約召喚` 表記で、1回300石
- パーティは最大5体
- バトル勝利後、階層に応じてモンスター仲間化抽選がある
- ダンジョンは5階層単位で並ぶ
- 配合は親2体を消費して、能力と個体値を継承した新しい個体を作る
- 装備には品質があり、品質ランクに応じて効果倍率と最大強化回数が変わる
- 装備強化には通常/上級/危険遺物があり、成功率と失敗リスクが違う
- `PrototypePartyBootstrapService.UnlockAllImplementedMonstersForPreview = true` が現在のプレビュー用挙動

本番チュートリアルでは、初回体験を壊さないためにプレビュー用の全解放は無効化する前提で考える。

## 物語コンセプト

### 仮タイトル

`封契の迷宮群`

既存プロジェクト名を変える必要はない。ゲーム内の世界観タイトル、章タイトル、ロゴ文言として使える名前。

### コアメッセージ

`倒したものを奪うのではなく、各地のダンジョンに散った契約を結び直し、探索隊を少しずつ強くする。`

この考え方にすると、既存システムを次のように説明できる。

- 召喚: 拠点の契約炉に残った契約片から眷属を呼び戻す
- 仲間化: 暴走していた契約を修復する
- 編成: 契約した眷属を探索隊として送り出す
- 装備: ダンジョンで見つけた遺物を鑑定し、眷属に持たせる
- 装備品質: 遺物に残った魔力純度。高品質ほど効果が高く、鍛えられる回数も多い
- 装備強化: 強化遺物を使い、遺物の刻印を深く彫り直す
- 配合: 2つの契約核を統合し、記憶と力を継承させる
- 周回: ダンジョン内の魔力濃度が変化するため、同じ階層にも何度も挑める
- 放置報酬: 眷属が拠点周辺や踏破済みダンジョンで拾ってくる残響資源

### 世界観

世界には、古い契約によって封じられたダンジョンが点在している。

プレイヤーは、拠点に残された `契約炉` を扱える新しい契約師。契約炉は、各ダンジョンに散った契約片、装備遺物、眷属の記録を読み取り、探索隊を送り出すための中継装置になっている。

目的は「単一ルートを上へ進む」ことではなく、性質の違うダンジョンを順に攻略し、失われた契約網を復旧すること。

各ダンジョンは5階層前後の短い探索区画として扱う。プレイヤーは新しいダンジョンへ進むほど、眷属、装備、配合、品質厳選、強化遺物の使い分けを覚えていく。

## 主要キャラクター

### プレイヤー

呼称: `契約師`

設定:

- 記憶の一部を失った状態で、古い探索拠点の契約炉前に現れる
- 自分で戦うのではなく、契約した眷属を導く
- なぜ契約炉を扱えるのかは中盤まで明かさない

ゲーム上の役割:

- 所持モンスター、編成、装備、召喚、配合を管理する存在
- プレイヤーレベルは契約師としての同期率

### 案内役

名前: `リュシカ`

設定:

- 拠点の契約炉に残っていた小さな案内人格
- 明るく支えるが、各ダンジョンの過去については断片的にしか覚えていない
- チュートリアルでは「よぉし！」系の既存ホーム文言と相性がいい

ゲーム上の役割:

- 初回チュートリアルの案内役
- ホームの短いガイド文を担当
- 章クリア時に次の目的を短く伝える

口調:

- 短く、前向き
- 専門用語を言ったあとにすぐ行動へ落とす
- 例: `よぉし、契約炉が反応しました。まずは一体、眷属を呼び戻しましょう。`

### 迷宮の残響

呼称: `黒い契約炉`

設定:

- 明確な人型キャラクターではなく、各ダンジョンの奥や契約炉から届く断片的な声
- 序盤は敵か味方か分からない
- 章の節目で短い不穏な一文だけ出す

使い方:

- バトル後や新ダンジョン解放時の短い演出に向く
- 長い会話にしない

## 章構成

既存の `BattleDungeonCatalog` の6ダンジョンに合わせる。

全体は単一ルート攻略ではなく、拠点から複数のダンジョンを順番に解放していく構成にする。コード上の `floor` は、ゲーム内では `ダンジョン階層` として扱う。

### 序章: 契約炉の目覚め

対象:

- 初回起動
- 初回召喚
- 初回編成
- 初回バトル

内容:

- プレイヤーが拠点の契約炉前で目覚める
- リュシカが契約炉の再起動を案内する
- 初めての眷属を呼び、探索隊を組み、最初のダンジョンへ向かう

到達目標:

- プレイヤーが「召喚したモンスターを編成して戦うゲーム」だと理解する

### 第1章: 見習いの五門洞

対象ダンジョン:

- `blight_cavern`
- 第1〜5階層

物語:

- 契約炉とつながる五つの小門を修復する
- 竜、機兵、岩像、剣士、魔導の契約片が見つかる

解放/学習:

- バトル
- 仲間化
- 図鑑
- 5体編成の意味

章クリア演出:

- 5つの小門が点灯し、次のダンジョンへの転移門が開く

### 第2章: 獣影の廃工廠

対象ダンジョン:

- `gear_crypt`
- 第6〜10階層

物語:

- 廃工廠の生産炉が暴走し、契約片と装備遺物が混ざり始める
- 単一の相手ではなく、複数種族の組み合わせに対応する必要が出る

解放/学習:

- 編成の入れ替え
- 前衛/後衛、物理/魔法の考え方
- 装備変更
- 装備品質の見方

章クリア演出:

- 廃工廠の炉が一つ止まり、装備鑑定と配合炉の封印が少し緩む

### 第3章: 古契約の地下書庫

対象ダンジョン:

- `curse_library`
- 第11〜15階層

物語:

- 過去の契約記録が眠る書庫
- 同じ眷属を育て切ることで、上位契約へ進めることが分かる

解放/学習:

- レベル上限
- 配合の予告
- 個体差、ロック、お気に入りの重要性
- 低品質装備と高品質装備の差

章クリア演出:

- リュシカが「契約は消えるのではなく、継承される」と説明する

### 第4章: 紅蓮竜道

対象ダンジョン:

- `ember_drake_pass`
- 第16〜20階層

物語:

- クラス2相当の強敵が階層の最後に現れる
- 配合、品質の良い装備、装備強化を使わないと突破が難しくなる

解放/学習:

- 配合
- 上位クラス
- ボス戦
- 強化遺物の使い分け

章クリア演出:

- 黒い契約炉が初めて明確に反応し、より危険な強化遺物の存在を示す

### 第5章: 星鉱の巨殿

対象ダンジョン:

- `star_ore_citadel`
- 第21〜25階層

物語:

- 星を含んだ鉱石が脈動する巨大ダンジョン
- 装備品質、装備強化、配合でパーティ全体を底上げする段階

解放/学習:

- 装備強化
- 品質厳選
- 周回報酬
- 永続強化

章クリア演出:

- 星鉱の転移路が復旧し、深淵区画への入口が開く

### 第6章: 深淵魔導回廊

対象ダンジョン:

- `abyssal_grimoire_spire`
- 第26〜30階層

物語:

- 深淵の術式が空間を歪める高難度ダンジョン
- 契約網の破損が事故ではなく、誰かが意図的に起こしたことが示される

解放/学習:

- 高レア契約
- 高難度周回
- 次シーズン/次章への引き

章クリア演出:

- 契約炉の奥から、プレイヤーの名前を知っている声が聞こえる

## 初回チュートリアル方針

### 体験時間

目安: 3〜5分

長くなる場合は、次の2つに分ける。

- 必須チュートリアル: 初回召喚、編成、初戦、報酬確認
- 任意チュートリアル: 装備、配合、図鑑、ショップ、ミッション

### 画面ロック方針

初回だけは必要最小限のボタン以外を押せないようにする。

ただし、長時間ロックしない。

- 召喚画面: 1回召喚だけ強調
- 編成画面: 自動編成または1体選択だけ強調
- ホーム画面: `冒険開始` を強調
- バトル画面: 基本は見守る。倍速や撤退は初回だけ隠してもよい
- リザルト画面: 報酬と仲間化結果を確認してホームへ戻る

### スキップ

初回の会話はスキップ可能にする。

ただし、操作誘導はスキップしても次の状態だけは整える。

例:

- 初回召喚をスキップした場合、チュートリアル用の初期眷属1体を付与
- 編成をスキップした場合、その眷属を1枠目に編成
- 初戦前にスキップした場合、ホームの通常導線に戻す

## チュートリアル詳細フロー

### T00: 新規データ作成

発生条件:

- `HasCompletedTutorial == false`
- `TutorialStep == 0`

処理:

- `PrototypePartyBootstrapService` の全解放は本番では無効化
- 初期石は300〜600に調整するか、チュートリアル専用の1回無料召喚を使う
- ホーム表示直後に序章会話を開始

表示文:

`……契約炉、再起動。よかった、まだ間に合います。`

`あなたはこの契約炉に選ばれた契約師です。まずは、失われた眷属を一体呼び戻しましょう。`

完了条件:

- 会話を閉じる

次:

- T01

### T01: ホームで召喚へ誘導

対象シーン:

- `HomeScene`

強調対象:

- `召喚` ボタン

表示文:

`契約片が一つ残っています。召喚で、最初の眷属を呼び戻しましょう。`

完了条件:

- `GachaScene` に遷移

次:

- T02

### T02: 初回召喚

対象シーン:

- `GachaScene`

強調対象:

- 無料/通常の1回召喚ボタン

表示文:

`契約炉に石を捧げると、ダンジョンに残った記録から眷属が応えてくれます。`

推奨仕様:

- 初回召喚はチュートリアル専用プールにする
- 候補はクラス1の5種族
- どれが出ても後続チュートリアルが成立するようにする

完了条件:

- `OwnedMonsters.Count >= 1`

召喚後表示文:

`契約成功です。この子が、あなたの最初の探索隊員になります。`

次:

- T03

### T03: 編成へ誘導

対象シーン:

- `HomeScene`

強調対象:

- `編成` ボタン

表示文:

`呼び戻しただけでは戦えません。編成に入れて、探索隊として送り出しましょう。`

完了条件:

- `FormationScene` に遷移

次:

- T04

### T04: 初回編成

対象シーン:

- `FormationScene`

強調対象:

- 所持モンスター1体目
- 編成枠1
- もしくは `自動編成` ボタンを追加する場合はそれを強調

推奨仕様:

- チュートリアルでは手動選択より `おすすめ編成` を使う方が事故が少ない
- 既存UIに自動編成がない場合、初回だけ `PartyMonsterInstanceIds[0]` に自動セットして説明だけ出す

表示文:

`探索隊は最大5体まで組めます。まずは一体で大丈夫。契約した眷属を先頭に置きましょう。`

完了条件:

- `PartyMonsterInstanceIds` に有効な個体IDが1つ以上入る

完了後表示文:

`よぉし、探索隊の準備完了です。最初のダンジョンへ向かいましょう。`

次:

- T05

### T05: 初回バトルへ誘導

対象シーン:

- `HomeScene`

強調対象:

- `冒険開始` ボタン

表示文:

`見習いの五門洞に、壊れた契約反応があります。戦って、契約片を回収しましょう。`

完了条件:

- `BattleScene` に遷移

次:

- T06

### T06: 初回バトル

対象シーン:

- `BattleScene`

方針:

- 初戦は勝率100%寄りにする
- プレイヤー操作を増やさず、セミオートバトルを見せる
- スキルがある場合、使用可能になった瞬間だけボタンを強調する

表示文:

`戦闘は探索隊が自動で進めます。あなたは流れを見て、必要な時に力を解放してください。`

スキル使用時表示文:

`光ったボタンは使える合図です。今なら一気に押し切れます。`

完了条件:

- 初回バトル勝利

次:

- T07

### T07: 初回リザルト

対象シーン:

- `BattleScene`

強調対象:

- 報酬欄
- 仲間化結果
- `ホームへ` ボタン

表示文:

勝利時:

`契約片を回収しました。ゴールドと経験値は、次の探索の準備に使えます。`

仲間化成功時:

`暴走していた契約が戻りました。新しい眷属が仲間に加わります。`

仲間化失敗時:

`契約反応はありましたが、今回は届きませんでした。同じ階層に再挑戦すれば、また機会があります。`

完了条件:

- ホームへ戻る

次:

- T08

### T08: ホームで強化/次の目的を説明

対象シーン:

- `HomeScene`

強調対象:

- 初回は `冒険開始`
- 報酬が足りていれば `永続強化` または `装備`

表示文:

`これで基本は大丈夫。ダンジョンは何度でも挑めます。勝てなくなったら、編成、装備、品質、強化を見直しましょう。`

完了条件:

- `HasCompletedTutorial = true`
- 通常ホームガイドへ切り替え

次:

- 通常プレイ

## 任意チュートリアル

必須チュートリアル後、条件を満たしたタイミングで短く出す。

### 装備チュートリアル

発生条件:

- 装備画面を初めて開く
- または新しい装備を入手した

表示文:

`ダンジョンで見つかる遺物は、眷属ごとに持たせられます。前に出る子には耐久、攻撃役には火力を伸ばす装備が向いています。`

完了条件:

- 装備を1つ変更
- または画面を閉じる

### 装備品質チュートリアル

発生条件:

- 品質違いの装備を初めて入手
- または `QualityRank >= 3` の装備を初めて確認

表示文:

`同じ名前の装備でも、品質が違うと力の伸び方が変わります。高品質な遺物ほど効果が高く、鍛えられる回数も多くなります。`

補足文:

`品質は コモン、アンコモン、レア、エピック、レジェンダリー の順に高くなります。迷ったら品質の高いものを残しましょう。`

完了条件:

- 品質表示を含む装備詳細を見る
- または画面を閉じる

### 装備強化チュートリアル

発生条件:

- 強化遺物を初めて入手
- または装備詳細で強化ボタンを初めて表示

表示文:

`強化遺物を使うと、装備の刻印を深くできます。品質が高い装備ほど、強化できる回数も多くなります。`

遺物説明:

- `通常遺物`: 確実に成功する。まずは大事な装備に使う
- `上級遺物`: 成功率は低いが伸びが大きい。失敗しても装備は残る
- `危険遺物`: 成功時の伸びは大きいが、失敗すると装備が消滅する

注意文:

`ロック中の装備は、危険な強化や売却の対象にしない方針にすると安心です。大事な装備は先にロックしましょう。`

完了条件:

- 通常遺物で1回強化する
- または強化画面を閉じる

### 図鑑チュートリアル

発生条件:

- 仲間化または召喚で2種類以上のモンスターを入手

表示文:

`図鑑には、取り戻した契約の記録が残ります。まだ空いている欄は、どこかのダンジョンに眠る契約です。`

完了条件:

- 図鑑を初めて開く

### 配合チュートリアル

発生条件:

- 同じクラス1を2体所持
- または親候補2体がレベル上限に到達

表示文:

`配合は別れではありません。二つの契約核を一つに束ね、記憶と力を次の姿へ継承する儀式です。`

注意文:

`お気に入りやロック中の眷属は、儀式に使われません。大切な子は必ずロックしておきましょう。`

完了条件:

- 配合プレビューを見る
- 初回は実行まで強制しない

### ショップ/石チュートリアル

発生条件:

- 召喚石が不足した状態で召喚画面を開く
- またはショップを初めて開く

表示文:

`石やゴールドは、契約と準備に使う資源です。毎日の報酬や探索でも少しずつ集まります。`

完了条件:

- 画面を閉じる

## ストーリーイベント一覧

実装時は `StoryEventDefinition` としてデータ化すると扱いやすい。

| ID | 発生条件 | タイトル | 表示文 |
| --- | --- | --- | --- |
| `story_prologue_wakeup` | 新規開始直後 | 契約炉の目覚め | `……契約炉、再起動。あなたが新しい契約師ですね。` |
| `story_first_summon_done` | 初回召喚後 | 最初の眷属 | `契約成功です。この子は、契約炉があなたに託した最初の記録です。` |
| `story_first_battle_win` | 第1階層初勝利 | 第1階層突破 | `契約片を回収しました。見習いの五門洞が、少しだけ静かになりました。` |
| `story_first_equipment_quality` | 品質付き装備を初確認 | 遺物の品質 | `同じ装備でも、残った魔力の純度で性能が変わります。品質の高い遺物は大切にしましょう。` |
| `story_first_equipment_enhance` | 初回装備強化成功 | 刻印強化 | `遺物の刻印が深くなりました。強化回数には限りがあります。どの装備を鍛えるか選びましょう。` |
| `story_dungeon_1_clear` | 第5階層突破 | 五門の点灯 | `五つの小門が戻りました。次のダンジョンへの転移門が開きます。` |
| `story_dungeon_2_clear` | 第10階層突破 | 廃工廠の停止 | `生産炉の暴走が止まりました。配合炉の封印に、ひびが入っています。` |
| `story_fusion_unlocked` | 配合条件達成 | 継承の儀式 | `契約は失われません。次の姿へ、記憶ごと継がれていきます。` |
| `story_dungeon_3_clear` | 第15階層突破 | 古契約の記録 | `各地のダンジョンは、何かを閉じ込めるためだけの場所ではなかったようです。` |
| `story_dungeon_4_clear` | 第20階層突破 | 黒い契約炉 | `奥で、誰かがあなたを待っています。名前を呼ぶ声がしました。` |
| `story_dungeon_5_clear` | 第25階層突破 | 星鉱の転移路 | `深淵区画への道が開きました。ここから先は、契約網の記憶そのものです。` |
| `story_dungeon_6_clear` | 第30階層突破 | 契約炉の奥 | `おかえり、契約師。契約炉はずっと、あなたを覚えていた。` |

## ホームガイド文言案

現在の `BuildHomeGuideText` は短くて良い。チュートリアル完了後は同じ枠を世界観付きの短文に差し替える。

### 通常時

`よぉし！\n次のダンジョン階層で契約片を探しに行きましょう。`

### 報酬あり

`探索隊が戻っています。\n放置報酬 {gold}G を回収できます。`

### クエスト報酬あり

`契約記録が更新されました。\n受け取れる報酬が{count}件あります。`

### 強化可能

`今なら契約や装備を強められます。\n準備してから次のダンジョンへ向かいましょう。`

### 高品質装備あり

`品質の高い遺物があります。\n装備画面で誰に持たせるか確認しましょう。`

### 強化遺物あり

`強化遺物が残っています。\n大事な装備の刻印を深められます。`

### 編成不足

`探索隊が空いています。\n編成で眷属を配置しましょう。`

### 召喚可能

`契約石が反応しています。\n新しい眷属を呼び戻せるかもしれません。`

## 装備品質/強化の設計

既存実装に合わせ、装備は単なるステータス上昇ではなく `ダンジョン産の遺物を鑑定し、良い品質のものを選び、限られた回数だけ鍛える` 体験として扱う。

### 品質

品質は遺物に残った魔力純度。

実装上は `QualityRank` と `EquipmentRarity` に対応し、表示名は次の5段階。

- `コモン`
- `アンコモン`
- `レア`
- `エピック`
- `レジェンダリー`

プレイヤー向け説明:

`品質が高い装備ほど、基礎効果が強くなり、強化できる回数も増えます。`

既存ロジック上の意味:

- 品質倍率は `1.0 + (品質ランク - 1) * 0.2`
- 最大強化回数は `4 + 品質ランク`
- ダンジョンドロップ時に品質抽選が行われる

### 強化回数

強化回数は、遺物に刻印を追加できる残り余白。

表示文言:

- `残り {remaining}/{max}回`
- 初回説明では `強化回数には限りがあります。高品質な装備ほど長く育てられます。` と伝える

チュートリアル上の狙い:

- 序盤は拾った装備を気軽に試す
- 中盤から「高品質装備を残す」「低品質装備はつなぎにする」を学ばせる
- 危険遺物を使う前にロックの意味を理解させる

### 強化遺物

既存の3種を、プレイヤーに次の性格で見せる。

| ID | 表示名 | 成功率 | 効果 | 失敗時 | 使いどころ |
| --- | --- | --- | --- | --- | --- |
| `relic_safe_ember` | 通常遺物 | 100% | 基礎効果 +5% | 消費のみ | 初回強化、大事な装備 |
| `relic_risky_ember` | 上級遺物 | 30% | 基礎効果 +10% | 装備は残る | 予備装備、伸ばしたい主力 |
| `relic_volatile_ember` | 危険遺物 | 10% | 基礎効果 +25% | 装備消滅 | 予備がある装備、終盤の挑戦 |

会心や速度付き装備の場合は固定ボーナスも加わるため、説明は次のように短くまとめる。

`会心や速度が付いた装備は、強化でその長所も少し伸びます。`

### ダンジョン報酬との接続

装備品質と強化遺物は、複数ダンジョン攻略の動機にする。

- 浅いダンジョン: 装備を拾って付け替える
- 混合ダンジョン: 品質違いを見比べる
- 中盤ダンジョン: 通常遺物で主力装備を強化する
- 高難度ダンジョン: 上級/危険遺物を使うか判断する

ホームガイドやリザルトでは、単に `装備を入手` ではなく次のように出すと分かりやすい。

- `レア品質の遺物を発見しました`
- `強化遺物を入手しました`
- `この装備は残り6回まで強化できます`
- `危険遺物は失敗時に装備を失います`

## 実装設計

### セーブデータ追加案

`PlayerSaveData` に追加する。

```csharp
public bool HasCompletedTutorial;
public string TutorialStepId;
public List<string> SeenStoryEventIds;
public List<string> SeenTutorialHintIds;
```

`PlayerProfile` にも同じ値を持たせる。

初期値:

- `HasCompletedTutorial = false`
- `TutorialStepId = "T00"`
- `SeenStoryEventIds = new List<string>()`
- `SeenTutorialHintIds = new List<string>()`

### 新規クラス案

#### `StoryTutorialService`

責務:

- 現在の `PlayerProfile` から表示すべきストーリー/チュートリアルを判定
- 表示済みイベントを記録
- 次の `TutorialStepId` へ進める
- スキップ時に最低限の状態を補正する

主なメソッド案:

```csharp
public static StoryTutorialEvent GetNextEvent(PlayerProfile profile, string sceneName);
public static void MarkStorySeen(PlayerProfile profile, string eventId);
public static void AdvanceTutorial(PlayerProfile profile, string completedStepId);
public static void CompleteTutorial(PlayerProfile profile);
public static void ApplySkipRecovery(PlayerProfile profile);
```

#### `TutorialOverlayController`

責務:

- 半透明マスク
- 吹き出し
- 強調枠
- 対象ボタン以外の入力抑制
- スキップボタン

実装メモ:

- 各シーンのCanvas直下にランタイム生成でよい
- 強調対象は最初から座標固定にせず、可能なら `TutorialTargetKey` コンポーネントで探す
- 自動生成UIが多いため、GameObject名に依存しすぎない

#### `TutorialTargetKey`

各ボタンに付ける軽い目印。

```csharp
public sealed class TutorialTargetKey : MonoBehaviour
{
    public string Key;
}
```

キー例:

- `home.gacha`
- `home.formation`
- `home.battle`
- `gacha.single_free`
- `formation.first_owned_monster`
- `formation.slot_1`
- `equipment.first_item`
- `equipment.quality_label`
- `equipment.enhance_button`
- `equipment.relic_safe`
- `equipment.relic_risky`
- `equipment.relic_volatile`
- `battle.skill_1`
- `result.return_home`

### 既存コードへの差し込み箇所

#### `Bootstrapper`

- `SaveManager.LoadOrCreate()` 後にチュートリアル初期値を補正
- `SeenStoryEventIds` がnullなら空リスト化

#### `HomeSceneController`

- `Start()` 後、`StoryTutorialService.GetNextEvent(profile, "HomeScene")` を確認
- `BuildHomeGuideText` でチュートリアル中の文言を優先
- ボタン生成時に `TutorialTargetKey` を付与

#### `GachaPanelController`

- 初回召喚中は1回召喚のみ有効化
- 召喚成功後に `T02` 完了
- 初回専用召喚プールを使う場合は `RunContract` の前段で分岐

#### `FormationSceneController`

- 所持モンスターが1体以上で、編成が空なら初回だけ自動セット可能
- 編成完了時に `T04` 完了
- 戻る時に保存

#### `EquipmentPanelController`

- 装備詳細に品質ラベルが表示されたタイミングで `tutorial_equipment_quality` を判定
- 強化遺物を1個以上持っている時、強化ランチャーに `tutorial_equipment_enhance` を判定
- 初回強化では `relic_safe_ember` を優先して案内
- `relic_volatile_ember` は初回説明だけ行い、使用は強制しない
- ロック中装備に危険遺物を使わせない既存仕様を、チュートリアル文言でも説明する

#### `BattleSceneController`

- 初回バトル開始時に説明を一度だけ出す
- 初回スキル点灯時に短いヒント
- 初勝利時に `T06` 完了

#### `ResultPanelController`

- 初回リザルトは報酬/仲間化説明を優先
- 装備や強化遺物が落ちた時は、品質/強化チュートリアルの予約フラグを立てる
- `returnHomeButton` に `TutorialTargetKey = "result.return_home"` を付与

## 初期バランス調整案

現在の新規データは `FreeGachaStones = 3000` で10連が可能。初回体験としては少し強い。

おすすめは次のどちらか。

### A案: 初回無料召喚方式

- 初期石は `0` または `300`
- チュートリアル中だけ `first_contract_ticket` を持たせる
- 1回召喚後にチケット消費

メリット:

- 課金石/無料石の説明を後回しにできる
- 初回導線が分かりやすい

### B案: 初期石300方式

- 初期石は `300`
- 1回召喚だけできる
- 10連は後で報酬として目指す

メリット:

- 実装が簡単
- 既存の石消費ロジックをそのまま使える

推奨はA案。実装を急ぐ場合はB案。

## 初回パーティ設計

初回召喚は完全ランダムでもよいが、チュートリアル事故を避けるなら以下のどれか。

### A案: 5種族からランダム

- 竜
- 機兵
- 岩像
- 剣士
- 魔導

メリット:

- プレイヤーごとに最初の相棒が違う

注意:

- どれが出ても第1階層に勝てる数値調整が必要

### B案: 固定初期眷属

- `monster_dragon_whelp` など、見た目が分かりやすい1体

メリット:

- チュートリアル文言と演出を作りやすい

注意:

- 初回の個性は薄くなる

### C案: 選択式

- 物理型
- 魔法型
- 耐久型

メリット:

- プレイヤーの愛着が出る

注意:

- UIと説明が増えるため、最初の実装では重い

推奨はA案。現在の5種族構成と相性がいい。

## UI/演出方針

### 会話表示

- 画面下部の吹き出し
- 1ページ最大2行
- 1イベント最大3ページ
- スキップ可能

### 強調表示

- 対象ボタンの周囲に細い光枠
- 周囲は軽く暗くする
- 矢印より光枠を優先

### キャラクター立ち絵

初期実装では必須ではない。

将来、案内役リュシカの小さなポートレートを追加するとホームの印象が強くなる。作成する場合はプロジェクト方針どおり image2 で生成する。

## 実装優先順

1. `PlayerSaveData` / `PlayerProfile` にチュートリアル保存項目を追加
2. `StoryTutorialService` を作成
3. ホームに簡易会話オーバーレイを追加
4. ホームの `召喚 -> 編成 -> 冒険開始` 誘導だけ通す
5. 初回召喚後、初回編成後、初回勝利後にステップ更新
6. 装備品質と通常遺物強化の任意チュートリアルを追加
7. 配合、図鑑、ショップなどの任意チュートリアルを追加
8. 章クリアのストーリーイベントを追加

## MVP完成条件

- 新規開始後、次に押すべきボタンが分かる
- 初回召喚ができる
- 召喚したモンスターが編成される
- 初回バトルに勝てる
- 報酬と仲間化の意味が分かる
- ホームに戻った時、次に何をするか分かる
- 初めて品質付き装備を見た時、品質と強化回数の意味が分かる
- 初めて強化遺物を使う時、成功率とリスクの違いが分かる
- チュートリアル完了後は通常導線を邪魔しない

## 追加で作ると良い文書/データ

- `STORY_EVENT_MASTER.md`: ストーリーイベントIDと本文の一覧
- `TUTORIAL_STEP_MASTER.md`: チュートリアルステップID、対象シーン、対象キーの一覧
- `L10N_JA_TUTORIAL.md`: 日本語文言だけを分離した調整用ファイル
